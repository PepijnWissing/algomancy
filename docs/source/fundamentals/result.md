(fundamentals-result-ref)=
## ScenarioResult
The `ScenarioResult` captures the raw output of an `Algorithm`. 
While the result itself contains the details of the "solution," it is often too dense for direct comparison.

For in-depth technical detail, refer to the {ref}`Scenario Result reference <result-ref>`.
