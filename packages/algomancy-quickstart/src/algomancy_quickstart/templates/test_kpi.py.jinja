"""PyTest skeleton for :class:`{{ class_name }}KPI`.

Generated by ``algomancy-quickstart``. Replace the placeholder
assertions with real expectations once the KPI calculation is filled in.
"""

from __future__ import annotations

import pytest

from algomancy_scenario import ScenarioResult, ImprovementDirection

from src.templates.kpi.{{ filename }}_kpi import {{ class_name }}KPI


@pytest.fixture
def kpi() -> {{ class_name }}KPI:
    return {{ class_name }}KPI()


@pytest.fixture
def sample_result() -> ScenarioResult:
    # TODO: Build a representative ScenarioResult once the KPI starts
    # depending on result contents.
    return ScenarioResult(data_id="test-data")


def test_kpi_construction_sets_metadata(kpi: {{ class_name }}KPI) -> None:
    assert kpi.name
    assert kpi.direction in (
        ImprovementDirection.HIGHER,
        ImprovementDirection.LOWER,
    )


def test_compute_returns_numeric(
    kpi: {{ class_name }}KPI, sample_result: ScenarioResult
) -> None:
    value = kpi.compute(sample_result)
    assert isinstance(value, (int, float))
