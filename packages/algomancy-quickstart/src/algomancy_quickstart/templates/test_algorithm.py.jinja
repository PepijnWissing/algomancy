"""PyTest skeleton for :class:`{{ class_name }}Algorithm`.

Generated by ``algomancy-quickstart``. These are starting-point tests
intended to be filled in once the algorithm logic is implemented —
they currently assert only that the algorithm wires up correctly and
returns a :class:`ScenarioResult`.
"""

from __future__ import annotations

import pytest

from algomancy_data import DataSource
from algomancy_scenario import ScenarioResult

from src.templates.algorithm.{{ filename }}_algorithm import (
    {{ class_name }}Algorithm,
    {{ class_name }}Params,
)


@pytest.fixture
def params() -> {{ class_name }}Params:
    return {{ class_name }}Algorithm.initialize_parameters()


@pytest.fixture
def algorithm(params: {{ class_name }}Params) -> {{ class_name }}Algorithm:
    return {{ class_name }}Algorithm(params=params)


@pytest.fixture
def empty_data() -> DataSource:
    # TODO: Replace with a representative DataSource fixture once the
    # algorithm depends on specific tables / columns.
    return DataSource(id="test-data")


def test_initialize_parameters_returns_params_instance() -> None:
    params = {{ class_name }}Algorithm.initialize_parameters()
    assert isinstance(params, {{ class_name }}Params)


def test_params_validate_does_not_raise_on_defaults(
    params: {{ class_name }}Params,
) -> None:
    # Default parameters are expected to be valid; replace this with
    # boundary-condition tests once you add real parameters.
    params.validate()


def test_run_returns_scenario_result(
    algorithm: {{ class_name }}Algorithm, empty_data: DataSource
) -> None:
    # TODO: Once the algorithm produces meaningful output, assert on the
    # contents of the returned ScenarioResult instead of just its type.
    result = algorithm.run(empty_data)
    assert isinstance(result, ScenarioResult)
    assert result.data_id == empty_data.id
