[project]
name = "algomancy-installer"
version = "0.1.2"
description = "Installation helper for the algomancy library"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
algomancy-installer = "algomancy_installer.main:main"

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"
